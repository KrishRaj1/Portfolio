# Shared components quick notes

- Projects are now rendered from `projects.js` using `ProjectCard.jsx`. Edit `projects.js` to add or change projects.
- Dark mode is enabled via Tailwind (`darkMode: 'class'`). Toggle in the navbar; class is applied to `<html>`.
- `Footer.jsx` added site-wide. Update links as needed.